# Copia este archivo a .env y ajusta los valores para tu entorno.

APP_HOST=0.0.0.0
APP_PORT=8000

# Token estatico provisional. Sustituye por una cadena generada aleatoriamente.
APP_TOKEN=cambia_este_token_por_uno_seguro

# Credenciales de acceso basico para generar tokens JWT.
ADMIN_USER=admin
ADMIN_PASS=admin123
READONLY_USER=viewer
READONLY_PASS=viewer123

# Configuracion del token JWT emitido por la API.
JWT_SECRET=define_un_secreto_muy_largo
JWT_TTL_SECONDS=43200

# Separa multiples origenes con comas. Usa * para permitir todos (solo en dev).
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Ruta al archivo que define servicios y unidades permitidas.
WHITELIST_PATH=config/whitelist.yaml

# Ruta del registro de dispositivos (multi-Pi).
DEVICES_REG_PATH=data/devices.yaml

# Intervalo sugerido en segundos para solicitar metricas desde el front-end.
METRICS_SUGGESTED_INTERVAL=3
